#include <stdio.h>
#include <time.h>
#include <stdlib.h>

/**
 * main - program that prints single digits of base '10'
 *
 * Return: 0 on success
 */
int main(void)
{

	char i;

	for (i = 0; i < 10; i++)
	{
		putchar(i);
	}

	return (0);
}
