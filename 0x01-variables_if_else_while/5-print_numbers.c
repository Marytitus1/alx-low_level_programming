#include <stdio.h>

/**
 * main - program that prints single digits of base '10'
 *
 * Return: 0 on success
 */
int main(void)
{

	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}

	return (0);
}
